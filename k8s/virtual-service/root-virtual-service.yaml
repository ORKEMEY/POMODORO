apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: root-virtual
spec:
  hosts:
    - root-service 
  http:
    - route:
        - destination:
            host: root-service 
      timeout: 5s
      retries:
        attempts: 3
        retryOn: 5xx
        perTryTimeout: 5s